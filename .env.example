# Server
PORT=3000

# Admin email to receive alerts (or leave blank to just log)
ADMIN_EMAIL=you@example.com

# Optional SMTP (for real email alerts)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=smtp-user@example.com
SMTP_PASS=supersecret

# Geolocation provider (for reference; the frontend uses ipapi.co fallback)
# If you plug a paid provider, add the key here.
GEO_API_PROVIDER=ipapi
GEO_API_KEY=
